#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * @argc: count number of arguuments
 * @argv: argument string
 *
 * Return: Always 0
 */
int main(__attribute__((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
