#include <stdio.h>
#include "main.h"

/**
 * _strpbrk - searches for any of a set of bytes
 *
 * @s: pointer to string
 * @accept: pointer to string
 *
 * Return: pointer to the byte that matches otherwise return NULL
 */
char *_strpbrk(char *s, char *accept)
{
}
