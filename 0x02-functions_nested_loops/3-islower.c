#include "main.h"

/**
 * _islower - checks for lowercase character
 *
 *@c: single lowercase
 *
 * Return: Always 1 if int c is lowercase else return 0
 */
int _islower(int c)
{
	if (c == 'a' && c <= 'z')
	{
		return (1);
		c++
	}
	else
	{
		return (0);
	}
}

